module.exports = {
    callback: ({ message, args, text, client, prefix, instance }) => {
            instance.commandHandler.commands.forEach((command) => {})
        } // just simply to disable the defult help command from wokcommands
}